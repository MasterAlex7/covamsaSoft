import { LargoTotalPipe } from './largo-total.pipe';

describe('LargoTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new LargoTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
