import { PipeEspesorPipe } from './pipe-espesor.pipe';

describe('PipeEspesorPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeEspesorPipe();
    expect(pipe).toBeTruthy();
  });
});
