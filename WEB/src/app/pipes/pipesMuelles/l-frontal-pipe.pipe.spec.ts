import { LFrontalPipePipe } from './l-frontal-pipe.pipe';

describe('LFrontalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LFrontalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
