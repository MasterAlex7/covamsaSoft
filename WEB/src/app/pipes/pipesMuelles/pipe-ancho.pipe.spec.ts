import { PipeAnchoPipe } from './pipe-ancho.pipe';

describe('PipeAnchoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeAnchoPipe();
    expect(pipe).toBeTruthy();
  });
});
